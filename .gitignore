# dependencies
node_modules/

# builds
dist/
build/
coverage/
.turbo/

# environment — secrets never enter the repo (§7)
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# docker volumes (if ever mapped locally)
.data/

# FAT32 corruption zombie — inaccessible phantom directory left by a failed
# rebase on 2026-07-17. The package now lives at packages/presets. Delete this
# entry once the directory is removed (reboot, or `chkdsk E: /f` as admin).
packages/config/
